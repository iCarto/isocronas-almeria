const IsocronasUrlParamsConfig = {
    queryParamsEnabled: true,
    params: [
        {
            key: "municipality",
            type: "action",
            scope: "map",
        },
        {
            key: "category",
            type: "filter",
            scope: "map",
        },
    ],
};

export default IsocronasUrlParamsConfig;
